# PagePipetteControl actionable control map (local candidates)

Scope: OEM SSD `decompiled_src/BioXPControlLib/PagePipetteControl.cs` wiring, `ClassPipetteCollection.cs:193–202,418–665,1418–1423`, and `ControlLib.cs:763–770,803–805,1324–1477,4865–4875`; integrated local robot/BMS candidates in `2026-09-25-pipette-remaining-work.md`. This is an offline source/contract audit, not installed or physical verification. The BMS advanced catalog consumes the robot's exact-route action specs through the existing catalog relay; the BMS pipette panel is a separate, narrower surface. A route being listed in a fixture does not establish that its command was exercised on hardware.

The all-functions target requires the source-composed operator functions below. Their individual primitives are not substitutes. The controlling completion work is PIP-C1 through PIP-C6 in `2026-09-25-pipette-remaining-work.md`, against robot `329c7a8` and BMS `8c46d8d`; this map is not permission for new gates, implementation or hardware action.

| OEM actionable input and effect | Candidate native contract and operator exposure | Disposition |
|---|---|---|
| Aspirate/Dispense: selected `chkPipette1..4`, positive volume/speed, cached-tip reconciliation and `SetTopSpeed`, then liquid operation (`ClassPipetteCollection.cs:418–531`). Dispense also deselects a lost-tip channel and actually parses the **aspirate** text boxes at 523. | Typed `/liquid/aspirate`, `/liquid/dispense`, `/liquid/set-top-speed`, `/liquid/tip-status` and advanced catalog; BMS Well pipetting sends selected channel/volume via native protocol. Explicit-speed overloads already set speed. | PIP-C3: outer diagnostic tip-query/selection behavior remains; do not rewrite the existing speed path. Preserve the narrower explicit-stroke API. The source textbox bug is not a new typed protocol contract. |
| Dispense All: for each of four channels with cached `m_PipetteHasTip`, send `DispenseAll`, wait group completion (533–555). | `/liquid/dispense-all` accepts typed `channels` and calls `transport.dispense_all`; catalog route. | Primitive present; source's cached-tip selection is not automatically equivalent to arbitrary supplied channels. |
| Query Last Error: `QueryErrorLog(1)` on all four (557–569). | `/liquid/error-log` typed `raw_byte`; `transport.query_error_log` already queries all four (`transport.py:1674–1677`). | Existing four-channel backend; PIP-C3 is fixed-byte-1 operator presentation/readback, not another query implementation. |
| Diagnoses: tests 0, 1, 2 in sequence (571–582), using cached tipped-channel eligibility (261–278). | `/liquid/diagnoses` typed `number`, transport `execute_diagnoses`; catalog route. | PIP-C3: ordered 0→1→2 and combined results remain. Also resolve the port's extra freshness/provenance/generation eligibility requirements; they are not the source cached predicate. |
| Initialize: `initiateGroup`, check status, potentially second `initiateGroup` and status (584–604). | `/liquid/init` is constructor-lifecycle semantics; `/liquid/reinitialize` is a different source operation. The transport already has a conditional second-init helper (`transport.py:1408–1418`). | PIP-C3: bind the diagnostic caller without constructor-only work or another caller's retry policy. The retry mechanism itself is not missing. |
| Eject: query each channel tip status and eject only selected channels reporting 1 (606–624). | `/liquid/tip` typed eject, `/liquid/eject-all` and `/liquid/tip-status`; catalog. | Primitives present; caller must compose OEM selection/query if claiming button equivalence. |
| Read Pressure: all four `readPressure` results (626–638). | `/liquid/pressure` POST and `/liquid/readback`; advanced catalog and panel live readback. | Present. Cached panel pressure is separately labeled. |
| Get Data: for each channel `retriveADPInformation`, `getData`, and file log (640–659). | `/liquid/data`, `/liquid/firmware` and `/liquid/readback` with `include_data`; advanced catalog/panel. | PIP-C3: assemble usable four-channel part/revision/firmware/data output from existing mechanisms. Do not recreate obsolete Windows logging or noncritical diagnostic bulk. |
| Combo tip type: `combTipType_SelectionChanged` calls `ClassPipetteCollection.loadTip(enum)` which **only sets `m_TipType`** (ControlLib 4865–4875; collection 1418–1423). | Robot `POST /liquid/pipette/tip-type` accepts 50/200/201, updates software `_tip_type` only, and advertises an operator catalog action. BMS mounted panel calls that catalog action and labels its cached readback separately from physical pickup. | Implemented locally; offline robot route and BMS mounted tests pass. No installed or physical verification. |
| Load Tip: parsed tray/well, `moveTo` tip-tray location, `lowerPipette(overpress?)`, query tips, optional Z home, clear dirty (ControlLib 1324–1368). `chkOverPress` and `chkLiftZ` feed this caller only. | BMS Well pipetting `load_tip` native step; physical lower/lift catalog actions; robot `manual_pipetting.py`. The BMS pipette-panel load-tip planner is no-motion, and its separate physical Load button invokes `/liquid/tip` only. | Physical well-addressed sequence exists in native workflow; **panel primitive and planner are not OEM Load Tip button parity**. Do not introduce a second movement path. |
| Set current Z for paired tray zLow (1370–1415). | Robot `/motion/oem/pipette/tip_tray_set` and BMS typed tray 1–4 Set, measured steps plus saved/active readback. | Present in candidate, saved revision takes effect on next ordinary startup, unlike OEM in-process table mutation. |
| Plunger Up/Down: `setZaxisCurrentmax(31)` then signed `moveSteps("z", n)` (1417–1433). | Typed `/motion/oem/manual/relative` Z and `oem.z.move_steps` catalog owner. | Relative motion present; caller's current setting 31 is not implied by a bare relative command. |
| Move to Waste: `moveTo(locationID 6,0,0)` (1435–1438). | Typed `oem.deck.move_to_location` with OEM waste location. | Present; travel does not imply ejection. |
| Detect Fluid: pool plate transfers/presses, pipette initialization, five ordered zOffset scans and success-path Park (1440–1469). | The separate `diagnostic_detect_fluid` native action and mounted BMS button invoke this five-station finite composition. `/liquid/fluid-detection` remains a single primitive, not the diagnostic. | Binding exists, but PIP-C2 must preserve source exceptions versus suppressed/ignored results, and PIP-C5 must verify actual dispatch-to-panel result propagation. Offline tests do not establish complete source or installed/physical equivalence. |
| Log Pressure: click saves `m_settingsWindow.LogPressure` via `saveOperationParameters` (1471–1477). | `GET/PATCH /liquid/pipette/settings` typed runtime setting; BMS `operation-parameters` relay and mounted flags editor. | Present. Do not put in PositionTable. |

`chkP1..4`, tip labels/status/pressure text, result text and standalone `chkOverPress`/`chkLiftZ` are display or subordinate inputs, not independent commands. `chkPipette1..4` select the liquid/eject channels; `txtAspVol/Speed`, `txtDisVol/Speed`, tray/cell and relative-step text are arguments, not separate actions. Neither `PipettesInstalled` BoardTest bookkeeping nor unconsumed checkboxes create a diagnostic gate.

## Remaining source compositions (not established by primitive availability)

1. **Required diagnostic coverage:** finish PIP-C3 using existing source primitives and owners. Individual test/query controls may remain, accurately named, but do not complete the original diagnostic callers. Last-error querying and conditional second-init support already exist; reuse them.
2. **Specific-well integration:** PIP-C1 connects selected-channel source `newloadTips`/`pressload`/`KeepTip` and `TipLocation` publication to the manual authoring path. Selecting a plunger is not head alignment. Keep manual tray/well Load Tip, source-selected loading, host-only tip type and no-motion planning distinct.
3. **Calibration and results:** PIP-C4 completes reset, comparison/restore and finalization; its active-versus-next-startup policy needs an explicit decision. PIP-C5 preserves complete and partial-save results across real dispatch. A pending comparison does not make currently saved values inert.
4. **Remaining pipette families:** PIP-C6 covers typed exposure of already implemented air/purge, source mixing, kept-tip, status/condition, timestamp, termination and heartbeat functions without rewriting their backends. Manual repeated-stroke Mix is not `mmix`/`mixAll`. Hardware placement/optics remain separately unverified, never a new software gate.
