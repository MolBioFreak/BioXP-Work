# PagePipetteControl actionable control map (local candidates)

Scope: OEM SSD `decompiled_src/BioXPControlLib/PagePipetteControl.cs` wiring, `ClassPipetteCollection.cs:193–202,418–665,1418–1423`, and `ControlLib.cs:763–770,803–805,1324–1477,4865–4875`; robot `fada138`, BMS `df44fef`. This is an offline source/contract audit, not installed or physical verification. The BMS advanced catalog consumes the robot's exact-route action specs through the existing catalog relay; the BMS pipette panel is a separate, narrower surface. A route being listed in a fixture does not establish that its command was exercised on hardware.

| OEM actionable input and effect | Candidate native contract and operator exposure | Disposition |
|---|---|---|
| Aspirate/Dispense: selected `chkPipette1..4`, positive volume/speed, tip-status query and `SetTopSpeed`, then liquid operation (`ClassPipetteCollection.cs:418–531`). OEM Dispense handler actually parses the **aspirate** text boxes at 523, not its displayed dispense boxes. | Typed `/liquid/aspirate`, `/liquid/dispense`, `/liquid/set-top-speed`, `/liquid/tip-status` and advanced catalog; BMS Well pipetting sends selected channel/volume via native protocol. | Primitives present; the UI's source textbox bug is not a new protocol contract. Do not claim composed diagnostic button parity (tip query and speed sequence) from a single liquid command. |
| Dispense All: for each of four channels with cached `m_PipetteHasTip`, send `DispenseAll`, wait group completion (533–555). | `/liquid/dispense-all` accepts typed `channels` and calls `transport.dispense_all`; catalog route. | Primitive present; source's cached-tip selection is not automatically equivalent to arbitrary supplied channels. |
| Query Last Error: `QueryErrorLog(1)` on all four (557–569). | `/liquid/error-log` typed `raw_byte`; catalog route. | Primitive present; for exact button, issue byte 1 as a four-channel query, not an arbitrary log request. |
| Diagnoses: tests 0, 1, 2 in sequence (571–582). | `/liquid/diagnoses` typed `number`, transport `execute_diagnoses`; catalog route. | Primitive present; exact one-click aggregate requires ordered 0→1→2 with per-test evidence, not one selected test. |
| Initialize: `initiateGroup`, check status, potentially second `initiateGroup` and status (584–604). | `/liquid/init` and `/liquid/reinitialize` and catalog; source-prepared initialization is also separate. | Existing initiation owner, but a single command is not proof of the button's conditional second attempt. |
| Eject: query each channel tip status and eject only selected channels reporting 1 (606–624). | `/liquid/tip` typed eject, `/liquid/eject-all` and `/liquid/tip-status`; catalog. | Primitives present; caller must compose OEM selection/query if claiming button equivalence. |
| Read Pressure: all four `readPressure` results (626–638). | `/liquid/pressure` POST and `/liquid/readback`; advanced catalog and panel live readback. | Present. Cached panel pressure is separately labeled. |
| Get Data: for each channel `retriveADPInformation`, `getData`, and file log (640–659). | `/liquid/data` POST (`query` optional), `/liquid/readback` with `include_data`; advanced catalog/panel. | Hardware-data mechanism present. OEM local logfile and assembled four-channel part/revision/firmware report are not represented by a single data call; do not claim logfile parity. |
| Combo tip type: `combTipType_SelectionChanged` calls `ClassPipetteCollection.loadTip(enum)` which **only sets `m_TipType`** (ControlLib 4865–4875; collection 1418–1423). | Transport `loadTip` at `pipette/transport.py:2441–2447` sets software `_tip_type`; no standalone typed route/catalog item. BMS planner's `tip_type` only builds a no-motion plan, and physical `load_tip` includes movement. | **Missing narrow software-only selection contract**, below. Never equate this selection with tip pickup. |
| Load Tip: parsed tray/well, `moveTo` tip-tray location, `lowerPipette(overpress?)`, query tips, optional Z home, clear dirty (ControlLib 1324–1368). `chkOverPress` and `chkLiftZ` feed this caller only. | BMS Well pipetting `load_tip` native step; physical lower/lift catalog actions; robot `manual_pipetting.py`. The BMS pipette-panel load-tip planner is no-motion, and its separate physical Load button invokes `/liquid/tip` only. | Physical well-addressed sequence exists in native workflow; **panel primitive and planner are not OEM Load Tip button parity**. Do not introduce a second movement path. |
| Set current Z for paired tray zLow (1370–1415). | Robot `/motion/oem/pipette/tip_tray_set` and BMS typed tray 1–4 Set, measured steps plus saved/active readback. | Present in candidate, saved revision takes effect on next ordinary startup, unlike OEM in-process table mutation. |
| Plunger Up/Down: `setZaxisCurrentmax(31)` then signed `moveSteps("z", n)` (1417–1433). | Typed `/motion/oem/manual/relative` Z and `oem.z.move_steps` catalog owner. | Relative motion present; caller's current setting 31 is not implied by a bare relative command. |
| Move to Waste: `moveTo(locationID 6,0,0)` (1435–1438). | Typed `oem.deck.move_to_location` with OEM waste location. | Present; travel does not imply ejection. |
| Detect Fluid: pool plate transfers/presses, pipette initialization, five ordered zOffset scans and success-path Park (1440–1469). | `/liquid/fluid-detection` and panel action are *single primitive*, not this diagnostic. | Missing full composed caller; tracked separately as remaining-work item 4. Do not represent the primitive as the whole button. |
| Log Pressure: click saves `m_settingsWindow.LogPressure` via `saveOperationParameters` (1471–1477). | `GET/PATCH /liquid/pipette/settings` typed runtime setting; BMS `operation-parameters` relay and mounted flags editor. | Present. Do not put in PositionTable. |

`chkP1..4`, tip labels/status/pressure text, result text and standalone `chkOverPress`/`chkLiftZ` are display or subordinate inputs, not independent commands. `chkPipette1..4` select the liquid/eject channels; `txtAspVol/Speed`, `txtDisVol/Speed`, tray/cell and relative-step text are arguments, not separate actions. Neither `PipettesInstalled` BoardTest bookkeeping nor unconsumed checkboxes create a diagnostic gate.

## Exact remaining native contracts (not implemented in this audit)

1. **Software tip-type selection:** add a strict enum `50 | 200 | 201` request, e.g. `POST /liquid/pipette/tip-type` with `{ "tip_type": 50 }`, under the existing pipette owner, calling the existing transport's software-only `loadTip(tip_type)`; return requested/current software type and an ordinary command receipt. Explicitly no tip-status assertion, movement, pickup, or inventory publication. Add the generated catalog entry and a typed BMS relay/control using the existing catalog invocation; keep it separate from `load_tip` and the no-motion planner. Retained application settings and restart/reset behavior must follow the current transport owner rather than inventing persistence.
2. **If exact one-click OEM diagnostics are desired:** define finite source-shaped ordered actions in the existing owner for `diagnoses(0,1,2)`, all-channel last error byte 1, and conditional initialize/eject, with child receipts and source partial failures. The current individual catalog routes suffice for individual tests/queries but do not make these composites. Preserve current execution behavior; do not add new admission gates or physical interlocks.
3. **Exact Load Tip/Detect Fluid UI parity:** bind the already existing native `load_tip` step to its addressed inputs; separately complete the source-ordered Detect Fluid composition under the existing protocol worker (remaining-work item 4). Do not reroute the planner's no-motion `tip_type` into a physical pickup or claim `/liquid/fluid-detection` is the five-station scan.
