# BioXP 3200 #206: remaining pipette work

**Scope:** Four-channel pipetting from addressed wells, tip handling, pipette diagnostics and pipette calibration. This updates *candidate status and remaining work*, not the OEM source facts in `2026-08-02-pipette-oem-gap-rectification-spec.md`. Its §9–16 status/gates and the disabled-mutation language of the historical `2026-08-16-pipette-oem-parity-execution-plan.md` are not current behavior or permission to add new refusals. Preserve existing OEM/controller interlocks and execution behavior. Evidence gaps are reported, not converted into new admission or success conditions.

## What exists in local candidate source

Robot `2699515abacffe963b12c173e6fb783e8776b9d8`; BMS `43537329028c13972dbf4314a048b802f37c748c`. **Neither is deployed or physically qualified.** A separate robot test-fixture change is uncommitted.

- BMS **Well pipetting** is mounted in the cockpit (`BioXpCockpit.tsx:1631–1634`). Its typed single/ordered steps submit through the existing BMS protocol relay to robot `/protocol/execute`. `manual_pipetting.py` binds actual selected location/well Move, calibrated in-place Lower/Lift, explicit-channel Aspirate/Dispense, and repeated-stroke Mix. Channels select plungers, **not** head alignment or source `TipLocation`; Mix is not OEM `mixAll`/`mmix`. No implicit tip pickup, initialization, sweep, cleanup or Park.
- `load_tip` is a physical tray/well move/lower/query/optional lift in the ordinary finite owner. `measure_fluid_height` runs the BR/Z/completion/height sequence at the **current** well, without an XY move or calibration save (`pipette/oem_calibration.py`). Neither manufactures tip inventory or a calibrated result.
- The separate OEM-prepared script path already has `ldtip`, `mmix`, ejection and a finite `checkTips` callback using ClungTips/shared camera (`api.py:11839–11891`, `oem_serial206_initialization.py:12481–12664`). Do not reimplement them as missing manual steps. Its source exception/Boolean and optical completion are distinct; the installed camera has not been qualified.
- Robot GET/PATCH `/motion/oem/calibration_settings` and the mounted BMS typed editor save **final** PositionTable `x/y/zLow/zDelta/inc_factor` values for the next ordinary startup. The editor shows active versus saved revisions and raw versus loader-normalized motion values; saving does not rebind, home, restart or move. This is not automatic calibration or the OEM manual **Set** button.
- Existing `/liquid/*` routes and the advanced typed catalog provide lower-level pipette controls. This does not establish that every diagnostic button's composed OEM behavior or its mounted use is covered.

Offline native/API and BMS mounted/relay tests exercised these candidate paths with hardware replaced. Ten exported BMS documents containing 41 actions parsed against the robot native contracts. The 15 formerly blocked lifecycle cases pass with a repaired **uncommitted** fixture, not yet regression-checked against its other consumers. One unrelated RGB assertion fails on the unchanged baseline. None of these results is an installed or wet transfer.

## Exactly what remains to implement

1. **Close the pipette diagnostic control map.** For actionable `PagePipetteControl` inputs only, trace mounted control → existing typed relay/catalog → robot handler → source effect. Reuse the existing init/reinit, pressure/error/diagnoses/data, eject/dispense-all, channel selection, relative Z and waste-travel owners where they actually fit. Bind missing **software-only** tip-type selection separately from physical pickup; expose pipette-relevant source flags through their existing Operation_parameters owner, not PositionTable PATCH (`runtime_state.py:428,507`). OEM `ControlLib.cs:1471–1477` writes LogPressure. `PipettesInstalled` is BoardTest bookkeeping, not a four-channel construction gate. Display-only and unconsumed checkboxes are not new commands. Do not add raw JSON/CAN controls.
2. **Manual tip-tray Set.** `ControlLib.cs:1370–1415` reads current Z. Tray 1/2 saves that value as zLow for **both** TECANRACK1/2; tray 3/4 saves both TECANRACK3/4. Implement one typed current-Z read and atomic paired saved-configuration update with measured/saved/active readback. A typed manually entered row is not this procedure. Current saved-revision semantics mean the change takes effect at *next ordinary startup*, unlike OEM in-process PositionTable mutation; label that difference, never claim immediate application.
3. **Executable repeated `zOffset` scan.** `ControlLib.cs:3387–3627` samples A then B in source order, with optional fresh-tip/trough prefill at speed 100, source move/publication, measurement, return, tip query/ejection and `(int)(mean + .5)` result. The existing one-well measurement and pure `calibration_samples`/adjustment arithmetic are building blocks, not the loop. Preserve source partial effects and report its inconsistent return-to-tray location publication separately from actual position; changing that publication or introducing a retry cap requires Christian's approval.
4. **Two distinct calibration callers.** Diagnostic **Detect Fluid** (`ControlLib.cs:1440–1469`) transfers/presses pool at TC23 then MS25, initializes pipettes, scans TC/MS/OC/RC/STRIP with `transferFluid=false, skipSteps=1`, mutates strip color X and Parks on the source success path, not in an unconditional finally. **`calwithFluid`** (`ControlLib.cs:2751–2850`; `ClassBioXPSettings.cs:6039–6138,6258–6271,6273–6542`) is the separate accepted-calibration program: Z acceleration 176 before its confirmation, default prefill/repeated scans, `adjustZ`-derived updates (TC/MS/OC measured height plus FLUID_TC_OFFSET; MS also PLLow; RC plus FLUID_RC_OFFSET; STRIP uses FLUID_STRIP_OFFSET and the source's 1500 subtraction, applying one relative delta across four strip rows), source liquid-calibration metadata and saves, comparison/accept-or-restore, conditional waste/ejection/Park cleanup, acceleration restoration. Compose under the existing native workflow owner and reuse deck, Z, pipette and saved-configuration owners; do not call an isolated height reading the wizard. The present saved-revision owner **cannot activate new geometry in-process**. Specify calculated saved revisions as next-startup configuration unless Christian approves a concrete live-activation/restore behavior; do not claim exact OEM `calwithFluid` equivalence with a pending revision.
5. **Pipette-specific inspection setting only where selected.** `CheckForStaticTipLoss`, `CheckSnapTips`, `LogPressure` and relevant consumed flags belong to their captured/runtime settings owner. Verify each actual consumer before offering a typed edit. Preserve the 3200 ClungTips recipe and report camera exposure/illumination limitations when that branch executes; do not import 3250 recipes, add a general template trainer, or turn the OEM's inspection exception into a refusal without approval.

**No replacement control layer.** Retain the existing transport, canonical protocol worker, source state, SQLite owners, BMS relay and manual UI. No second scheduler, PyLabRobot controller, inferred tip custody, generic firmware tools or unrelated thermal/chiller/gripper calibration.

## Evidence and delivery, separate from command admission

- Offline exercise new compositions through their actual finite/native handlers, canonical movement/liquid transports, SQLite state and fixed BMS relay/mounted controls with only hardware I/O replaced. Verify paired writes, saved/active startup readback, ordered children, partial failure/Stop and original source exception behavior. Run the repaired fixture's other consumers before committing it. Report baseline-only failures separately.
- Integrate onto the supported robot/BMS lines and verify the merged trees before any authorized publication/deployment. Read back the served revisions, mounted control/route and calibration revision. Local commits or mock tests are not a running service.
- A bounded **physical** run needs Christian's authorization. Then report actual pickup, alignment, channel transfer, height/tip inspection and calibration after ordinary restart separately from controller receipts. Materials/tolerances and any live-activation decision belong to that run; missing physical proof is a reporting limit, **not a new software interlock**.
