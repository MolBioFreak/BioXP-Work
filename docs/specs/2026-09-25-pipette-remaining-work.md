# BioXP 3200 #206: remaining pipette work

**Scope:** Four-channel pipetting from addressed wells, tip handling, pipette diagnostics and pipette calibration. This updates *candidate status and remaining work*, not the OEM source facts in `2026-08-02-pipette-oem-gap-rectification-spec.md`. Its §9–16 status/gates and the disabled-mutation language of the historical `2026-08-16-pipette-oem-parity-execution-plan.md` are not current behavior or permission to add new refusals. Preserve existing OEM/controller interlocks and execution behavior. Evidence gaps are reported, not converted into new admission or success conditions.

## Integrated local candidate (not installed)

Robot branch `feat/oem-well-pipetting`; BMS branch `integrate/pipette-bms-dev-20260925`. **Neither is deployed or physically qualified.** Earlier candidate hashes and fixture status are superseded.

- BMS **Well pipetting** is mounted in the cockpit (`BioXpCockpit.tsx:1631–1634`). Its typed single/ordered steps submit through the existing BMS protocol relay to robot `/protocol/execute`. `manual_pipetting.py` binds actual selected location/well Move, calibrated in-place Lower/Lift, explicit-channel Aspirate/Dispense, and repeated-stroke Mix. Channels select plungers, **not** head alignment or source `TipLocation`; Mix is not OEM `mixAll`/`mmix`. No implicit tip pickup, initialization, sweep, cleanup or Park.
- `load_tip` is a physical tray/well move/lower/query/optional lift in the ordinary finite owner. `measure_fluid_height` runs the BR/Z/completion/height sequence at the **current** well, without an XY move or calibration save (`pipette/oem_calibration.py`). Neither manufactures tip inventory or a calibrated result.
- The separate OEM-prepared script path already has `ldtip`, `mmix`, ejection and a finite `checkTips` callback using ClungTips/shared camera (`api.py:11839–11891`, `oem_serial206_initialization.py:12481–12664`). Do not reimplement them as missing manual steps. Its source exception/Boolean and optical completion are distinct; the installed camera has not been qualified.
- Robot GET/PATCH `/motion/oem/calibration_settings` and the mounted BMS typed editor save **final** PositionTable `x/y/zLow/zDelta/inc_factor` values for the next ordinary startup. The editor shows active versus saved revisions and raw versus loader-normalized motion values; saving does not rebind, home, restart or move. This is not automatic calibration or the OEM manual **Set** button.
- Existing `/liquid/*` routes and the advanced typed catalog provide lower-level pipette controls. This does not establish that every diagnostic button's composed OEM behavior or its mounted use is covered.
- The pipette diagnostic control map in `2026-09-25-pipette-diagnostic-control-map.md` distinguishes actionable OEM buttons from existing lower-level routes. Software tip-type selection (T50/T200/T201), paired current-Z tray Set, and consumed runtime flags are typed robot actions with mounted BMS controls; tip-type selection does not physically pick up a tip.
- `loadTips(T50, forceNewTip=true)` has a finite owner bound to source-ordered tip query, optional forced ejection, tray pickup, and inventory/receipt publication. The native `source_fluid_offset` scans A then B, with optional source prefill, repeated height measurements, and distinct nested occurrences under that owner. Its return-to-tray move and later scanned-station location publication remain distinct source effects.
- The diagnostic `sourceDiagnosticDetectFluid` and calibration `sourceCalwithFluid` are distinct native manual actions, each under one finite owner. The former performs the five source stations, the latter runs source reset/prefill, saves each measured offset revision, restores Z acceleration, and exposes saved-revision/pending-restart evidence. BMS has separate controls and renders measurements; the installed `/liquid/fluid-detection` primitive is not misrepresented as the diagnostic composition.

Offline, the integrated robot candidate passed 153 fluid/manual/calibration/tip-inspection tests (11 skipped) plus 92 pipette/lifecycle-owner tests (59 skipped). The mounted BMS suite passed 22 tests, including the real relay route for each new fluid action; its exported requests parsed through `ProtocolDocument.from_payload` and the robot finite compiler. The repaired lifecycle fixture is committed and its other pipette consumers were exercised. The `loadTips` lost-Z retry and source ClungTips camera-false ejection were exercised through real provider/CV/SQLite owners with synthetic camera frames and hardware transport replaced; installed optics remain unverified. None of this is an installed or wet transfer.

## Remaining in-scope binding and evidence

1. **Calibration comparison/restore:** OEM `calwithFluid` compares a previous calibration and lets the operator accept or restore. The new finite run records an unresolved choice on already-calibrated machines and durable per-plate revisions; the settings owner already has an atomic restore primitive, but no operator comparison/choice binding exists. BMS explicitly labels this limitation. Do not auto-accept the pending revision, silently restore, or claim OEM-equivalent in-process geometry: saved values apply on the next ordinary startup. Any live activation or concurrent-write policy needs Christian's exact approval; do not make missing comparison proof a new motion/refusal gate.
2. **Optional exact diagnostic-button parity:** Existing typed init, four-channel error/pressure/data/diagnoses, conditional eject/Dispense All and relative Z owners remain individual controls. The source one-click conditional initialize/diagnoses(0→1→2)/last-error/eject compositions are not all reproduced. Keep this distinction in the diagnostic map; compose only if exact buttons, rather than the existing individual controls, are required.

**No replacement control layer.** Retain the existing transport, canonical protocol worker, source state, SQLite owners, BMS relay and manual UI. No second scheduler, PyLabRobot controller, inferred tip custody, generic firmware tools or unrelated thermal/chiller/gripper calibration.

## Evidence and delivery, separate from command admission

- The integrated candidate has connected finite/native handler tests with canonical movement/liquid transports and both SQLite owners, including five-station success, partial saved revisions, tip retry and camera-false ejection. Calibration comparison/restore remains unbound; do not present skipped cases as passes.
- Before any authorized deployment, verify the actual served robot/BMS revisions, mounted controls/relay, and active versus saved calibration revision. Local commits and offline tests are not a running service.
- A bounded **physical** run needs Christian's authorization. Then report actual pickup, alignment, channel transfer, height/tip inspection and calibration after ordinary restart separately from controller receipts. Materials/tolerances and any live-activation decision belong to that run; missing physical proof is a reporting limit, **not a new software interlock**.
